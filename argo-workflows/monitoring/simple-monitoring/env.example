# Environment Configuration
ENVIRONMENT=development  # development, staging, production

# Application Settings
PORT=8000
ARGO_NAMESPACE=argo

# Database Configuration (SQLite by default, PostgreSQL for production)
DATABASE_TYPE=sqlite  # sqlite, postgresql
DATABASE_PATH=/data/workflow_metrics.db

# PostgreSQL Settings (only needed if DATABASE_TYPE=postgresql)
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_DB=argo_monitor
POSTGRES_USER=argo_user
POSTGRES_PASSWORD=argo_pass

# Kubernetes Configuration
KUBECONFIG=/root/.kube/config  # Path to kubeconfig file

# Collection Settings
COLLECTION_INTERVAL_SECONDS=60
MAX_WORKFLOWS_PER_COLLECTION=100

# Security (for production)
SECRET_KEY=your-secret-key-here
ALLOWED_HOSTS=localhost,127.0.0.1,your-domain.com

# Cloud vs On-Prem Specific Settings
# For Cloud Deployment:
# - Use managed databases (PostgreSQL RDS, etc.)
# - Set ENVIRONMENT=production
# - Configure proper ALLOWED_HOSTS

# For On-Prem Deployment:
# - Can use SQLite for simplicity
# - Set ENVIRONMENT=on-premises
# - Configure internal network access 